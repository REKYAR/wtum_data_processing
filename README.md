# wtum_data_processing

how to use loaders.py:
download and unzip utk-face and facial-age, create data_merged directory in the directory where unzipped datasets reside, run loaders.py from the same directory.
